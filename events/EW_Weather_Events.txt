add_namespace = EW_weather_events

########Universal Event############
## RUSSIAN WINTER
country_event = {
	id = EW_weather_events.1
	title = EW_weather_events.1.t
	desc = EW_weather_events.1.d
	picture = GFX_report_event_Moscow_Country
	is_triggered_only = yes
	option = {
		name = "OK"
		#Weather
		if = {
			limit = {
				NOT = { SOV = { has_idea = EW_General_Winter } }
			}
			SOV = {
				add_timed_idea = {
					idea = EW_General_Winter
					days = 125
				}
			}
		}
		every_country = {
			limit = {
				any_state = { is_core_of = SOV is_controlled_by_ROOT_or_ally = yes }
				has_war_with = SOV
				NOT = { has_idea = EW_General_Winter_Attacker }
			}
			add_timed_idea = {
				idea = EW_General_Winter_Attacker
				days = 125
			}
		}
		clr_global_flag = RON_weather_report_in_progress
	}
}